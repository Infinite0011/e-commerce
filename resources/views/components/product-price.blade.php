<div {{ $attributes }}>
    <span class="text-3xl font-extrabold text-[#333]">${{ round($price, 2) }}</span><span class="pl-2.5 text-sm text-[#737373] font-medium">each & ${{ $pricePerCapsule }} per capsule</span>
</div>
